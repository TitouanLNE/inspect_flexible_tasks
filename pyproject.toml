[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inspect_flexible_tasks"
version = "0.1.0"
dependencies = [
    "inspect_ai",
    "inspect_evals"
]

[project.entry-points."inspect_ai.extensions"]
flexible_tasks = "flexible_tasks"
